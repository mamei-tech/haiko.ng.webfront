/***
 * This enum is used to specify to a component how it should specialize / adapt according to the entity that is defined.
 * This helps to tailor the components to specific cases.
 */
export enum EntityTypes {
    Default = 0,
    Staff = 1
}
